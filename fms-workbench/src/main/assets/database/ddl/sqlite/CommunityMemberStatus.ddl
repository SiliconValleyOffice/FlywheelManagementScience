CREATE  TABLE `CommunityMemberStatus` (
  `community_member_status` CHAR(8) NOT NULL ,
  `name` VARCHAR(24) NOT NULL ,
  `description` VARCHAR(128) NOT NULL ,
  PRIMARY KEY (`community_member_status`) );
